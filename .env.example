# =============================================================================
# Database (REQUIRED)
# =============================================================================

# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@localhost:5432/homepage

# =============================================================================
# Session (REQUIRED)
# =============================================================================

# Secret used to encrypt session cookies. Use a long random string.
# Generate one with: openssl rand -base64 32
SESSION_PASSWORD=ChangeThisBeforeShippingToProdOrYouWillBeFired

# =============================================================================
# Weather (optional — defaults to London if not set)
# Find your coordinates at: https://www.latlong.net
# =============================================================================

VITE_WEATHER_LATITUDE=51.5074
VITE_WEATHER_LONGITUDE=-0.1278
VITE_WEATHER_TIMEZONE=Europe/London
VITE_WEATHER_LOCATION=London, United Kingdom

# =============================================================================
# Google OAuth (optional — needed for Calendar, YouTube, and Todos widgets)
# Create credentials at: https://console.cloud.google.com/apis/credentials
# Enable: Google Calendar API, YouTube Data API v3, Google Tasks API
# =============================================================================

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# =============================================================================
# Spotify (optional — needed for Spotify widget)
# Create an app at: https://developer.spotify.com/dashboard
# =============================================================================

SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://localhost:3000/api/auth/spotify/callback

# =============================================================================
# GitHub (optional — needed for GitHub contributions widget)
# Create a personal access token at: https://github.com/settings/tokens
# No special scopes needed (public data only)
# =============================================================================

GITHUB_TOKEN=
GITHUB_USERNAME=

# =============================================================================
# Zipline (optional — image hosting for uploads)
# Requires a self-hosted Zipline instance: https://zipline.diced.sh
# =============================================================================

VITE_ZIPLINE_API_ENDPOINT=https://your-zipline-instance.com
VITE_ZIPLINE_API_TOKEN=

# =============================================================================
# Coolify (optional — needed for Coolify server status widget)
# Requires a Coolify instance: https://coolify.io
# =============================================================================

COOLIFY_TOKEN=
COOLIFY_URL=https://your-coolify-instance.com

# =============================================================================
# Romm (optional — needed for Romm retro gaming widget)
# Requires a Romm instance: https://github.com/rommapp/romm
# =============================================================================

ROMM_USERNAME=
ROMM_PASSWORD=
VITE_PUBLIC_ROMM_URL=https://your-romm-instance.com
